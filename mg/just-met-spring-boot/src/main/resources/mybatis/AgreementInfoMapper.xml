<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="just.met.springboot.Squirrel状态机.dao.AgreementInfoDao">

    <resultMap type="just.met.springboot.Squirrel状态机.entity.AgreementInfo" id="AgreementInfoMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="status" column="status" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="selectById" resultMap="AgreementInfoMap">
        select
          id, name, status
        from wmg_test.agreement_info
        where ID = #{id}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="selectList" resultMap="AgreementInfoMap">
        select
        id, name, status
        from wmg_test.agreement_info
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <!--插入数据-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into wmg_test.agreement_info
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null and name != ''">
                name ,
            </if>
            <if test="status != null and status != ''">
                status ,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="name != null and name != ''">
                #{name} ,
            </if>
            <if test="status != null and status != ''">
                #{status} ,
            </if>
        </trim>
    </insert>

    <!--批量插入数据-->
    <insert id="insertBatch" parameterType="java.util.List">
        insert into wmg_test.agreement_info
        (name, status)
        values
        <foreach collection="list" item="item" index="index" open="" close="" separator=",">
            (#{item.name}, #{item.status})
        </foreach>
    </insert>


    <!--通过主键修改数据-->
    <update id="update">
        update wmg_test.agreement_info
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="status != null and status != ''">
                status = #{status},
            </if>
        </set>
        where ID = #{id}
    </update>

    <!--批量更新数据-->
    <insert id="updateBatch" parameterType="java.util.List">
        <foreach collection="list" item="item" index="index" open="" close="" separator=";">
            update wmg_test.agreement_info
            <trim prefix="set" suffixOverrides=",">
                <if test="item.name != null and item.name != ''">
                    name = #{item.name},
                </if>
                <if test="item.status != null and item.status != ''">
                    status = #{item.status},
                </if>
            </trim>
            where ID = #{item.id}
        </foreach>
    </insert>

    <!--通过主键物理删除-->
    <delete id="deleteById">
        delete from wmg_test.agreement_info where ID = #{id}
    </delete>

</mapper>