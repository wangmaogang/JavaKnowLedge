<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="just.met.springboot.redis.dao.RedisNumDao">

    <select id="getNowDayMaxNum" resultType="java.lang.Integer">
        SELECT
        substring(manager_num, -4, 4) num
        FROM
        calibration_daily_management_info
        WHERE
        TO_DAYS(user_id_create) = TO_DAYS(NOW())
        ORDER BY num DESC
        LIMIT 1
    </select>

</mapper>